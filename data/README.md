# Data directory

The study dataset is not distributed with this repository.

Authorized users may place a locally prepared dataset at `data/input_data.csv`. It must contain the column names listed in `data_dictionary.csv`, with coding and preprocessing consistent with the manuscript and study protocol.

Do not commit clinical data, patient identifiers, train/test datasets, prediction files, outputs, or trained model files to the public repository.
